[pytest]
addopts = -q --import-mode=importlib
# Limit discovery to module test roots to avoid cross-package conftest clashes
testpaths =
    editorial-service/tests
    kolegium/ai_writing_flow/tests
    topic-manager/tests
